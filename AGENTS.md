# AGENTS.md

## Global Policy
- All code changes must be traceable to spec refs.
- Prefer deterministic checks before autonomous retries.
- Capture every recurring failure as a durable guard.

## Learned Guards
- Lessons are appended automatically by `/rpi-auto memory` (or close-flow auto update).
- Keep this section versioned in git so prevention rules can accumulate over time.
